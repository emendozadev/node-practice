extends layouts/main-layout.pug
block styles
    link(rel="stylesheet" href="/static/css/product.css")
block content
    main
        if products.length > 0
            .grid
                each product in products
                    include template/product-item.pug
        else 
            h1 No Products